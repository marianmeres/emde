---
title: Projects
description: Selected work and side projects.
---

# Projects

## Design Token System

A comprehensive design token pipeline that generates CSS custom properties,
Tailwind configs, and Figma variables from a single source of truth.

## Static Site Generator

A zero-config markdown-to-HTML tool with layout inheritance,
hierarchical metadata, and built-in SEO helpers.

## Component Library

A framework-agnostic UI component library built on web standards.
Accessible by default, themeable via CSS custom properties.
