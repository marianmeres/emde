---
title: vanilla() helper demo
---

# `vanilla()` helper demo

This page inlines [`@marianmeres/vanilla`](https://jsr.io/@marianmeres/vanilla)
into the document head via the `vanilla()` template helper (mirroring how
`reboot()` inlines CSS). The counter below is a `<template>` cloned at runtime
and wired with `observable` / `reactTo` / `delegate`:
